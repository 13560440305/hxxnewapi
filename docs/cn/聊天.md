# 聊天

本页说明聊天/对话相关接口：**Chat Completions 格式**、**Responses 格式**。

**快速跳转**：[Chat Completions 格式](#chat-completions) · [Responses 格式](#responses)

---

<span id="chat-completions"></span>
## Chat Completions 格式

根据对话历史创建模型响应。支持流式和非流式响应。兼容 OpenAI Chat Completions API。

**请求方式**：`POST`  
**路径**：`/v1/chat/completions`

### Authorization

使用 Bearer Token 认证。

- **格式**：`Authorization: Bearer <token>`（例如 `Authorization: Bearer sk-xxxxxx`）
- **位置**：`header`

### Request Body

`Content-Type: application/json`

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| model | string | 是 | 模型 ID |
| messages | array\<object\> | 是 | 对话消息列表 |
| temperature | number | 否 | 采样温度，默认 1，范围 0～2 |
| top_p | number | 否 | 核采样参数，默认 1，范围 0～1 |
| n | integer | 否 | 生成数量，默认 1 |
| stream | boolean | 否 | 是否流式响应，默认 false |
| stream_options | object | 否 | 流式选项 |
| stop | string \| array\<string\> | 否 | 停止序列 |
| max_tokens | integer | 否 | 最大生成 Token 数 |
| max_completion_tokens | integer | 否 | 最大补全 Token 数 |
| presence_penalty | number | 否 | 默认 0，范围 -2～2 |
| frequency_penalty | number | 否 | 默认 0，范围 -2～2 |
| logit_bias | object | 否 | — |
| user | string | 否 | — |
| tools | array\<object\> | 否 | — |
| tool_choice | string \| object | 否 | — |
| response_format | object | 否 | — |
| seed | integer | 否 | — |
| reasoning_effort | string | 否 | 推理强度，可选 `low` \| `medium` \| `high` |
| modalities | array\<string\> | 否 | — |
| audio | object | 否 | — |

### Response

- **200**：成功返回 `application/json`，包含 `id`、`object`、`created`、`model`、`choices`（含 `message`、`finish_reason`）、`usage` 等。
- **400** / **429**：错误时返回 `{ "error": { "message", "type", "param", "code" } }`。

### 示例

<!-- tabs:start -->
#### **cURL**
```bash
curl -X POST "https://www.hxxopen.com/v1/chat/completions" \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o-mini",
    "messages": [
      { "role": "system", "content": "你是一个助手。" },
      { "role": "user", "content": "你好" }
    ]
  }'
```

#### **JavaScript**
```js
fetch("https://www.hxxopen.com/v1/chat/completions", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <API_KEY>"
  },
  body: JSON.stringify({
    model: "gpt-4o-mini",
    messages: [
      { role: "system", content: "你是一个助手。" },
      { role: "user", content: "你好" }
    ]
  })
})
```

#### **Python**
```py
import requests

response = requests.post(
    "https://www.hxxopen.com/v1/chat/completions",
    headers={
        "Authorization": "Bearer <API_KEY>",
        "Content-Type": "application/json",
    },
    json={
        "model": "gpt-4o-mini",
        "messages": [
            {"role": "system", "content": "你是一个助手。"},
            {"role": "user", "content": "你好"},
        ],
    },
)
print(response.json())
```
<!-- tabs:end -->

---

<span id="responses"></span>
## Responses 格式

OpenAI Responses API，用于创建模型响应。支持多轮对话、工具调用、推理等功能。

**请求方式**：`POST`  
**路径**：`/v1/responses`

### Authorization

使用 Bearer Token 认证。

- **格式**：`Authorization: Bearer <token>`（例如 `Authorization: Bearer sk-xxxxxx`）
- **位置**：`header`

### Request Body

`Content-Type: application/json`

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| model | string | 是 | 模型 ID |
| input | string \| array\<object\> | 否 | 输入内容，可为字符串或消息数组 |
| instructions | string | 否 | 指令 |
| max_output_tokens | integer | 否 | 最大输出 Token 数 |
| temperature | number | 否 | — |
| top_p | number | 否 | — |
| stream | boolean | 否 | — |
| tools | array\<object\> | 否 | — |
| tool_choice | string \| object | 否 | — |
| reasoning | object | 否 | — |
| previous_response_id | string | 否 | — |
| truncation | string | 否 | 可选 `auto` \| `disabled` |

### Response

- **200**：成功返回 `application/json`，包含 `id`、`object`、`created_at`、`status`、`model`、`output`、`usage` 等。

### 示例

<!-- tabs:start -->
#### **cURL**
```bash
curl -X POST "https://www.hxxopen.com/v1/responses" \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o-mini",
    "input": "你好"
  }'
```

#### **JavaScript**
```js
fetch("https://www.hxxopen.com/v1/responses", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <API_KEY>"
  },
  body: JSON.stringify({
    model: "gpt-4o-mini",
    input: "你好"
  })
})
```

#### **Python**
```py
import requests

response = requests.post(
    "https://www.hxxopen.com/v1/responses",
    headers={
        "Authorization": "Bearer <API_KEY>",
        "Content-Type": "application/json",
    },
    json={
        "model": "gpt-4o-mini",
        "input": "你好",
    },
)
print(response.json())
```
<!-- tabs:end -->
